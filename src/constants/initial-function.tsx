const functionString = `// Functions should have the following format, with at least one argument
function myFunc(arr) {
    // time complexity: O(n)
    for (let element of arr) {
        console.log(element);
    }
}

myFunc([1,2,3]); // alter the length of the input list to track differences between lengths
// press submit to measure function execution time
`;

export default functionString;